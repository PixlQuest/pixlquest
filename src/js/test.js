define("test", function () {
    //You can name this function here,
    //which can help in debuggers but
    //has no impact on the module name.
    var name = "sponge bob";
    // return function nottest(first, last) {
    //     this.first = first;
    //     this.last = last;
    // };
    return name;
});
